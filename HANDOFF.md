# HANDOFF — club432.com (новий сайт Клубу 432)

> Передача контексту для продовження роботи в новій сесії. Оновлено 2026-08-20.

## Що це і де воно

- **Живий сайт:** https://club432.com (головна + `/metod-432`, `/oferta`, `/privacy-policy`, `/retreat-carpathians`)
- **Проєкт:** `~/work/retreat-landing/` — Next.js 16 + Tailwind 4 + Framer Motion + (R3F/Lenis для окремих сторінок)
- **Vercel:** проєкт `retreat-landing` (id `prj_YY8ivjANPpU6eY17aRSZt9xNXPri`, team `team_7sIAmRWYNgwqmLVx5caZSVAA`), залогінений CLI як `prostopeopleua-png`. Домен `club432.com` (+ www) причеплений сюди, DNS керується у Vercel (NS у реєстратора HOSTiQ → vercel-dns).
- **GitHub:** `prostopeopleua-png/club432-retreat-landing` (gh активний акаунт `prostopeopleua-png`).

## ⚠️ Деплой
Git-push у GitHub **НЕ** тригерить авто-деплой Vercel. Деплоїти вручну:
```bash
cd ~/work/retreat-landing && vercel --prod --yes
```
(зазвичай я роблю: `git add -A && git commit … && git push` + `vercel --prod --yes`).

## Що вже зроблено (стан головної)

Головна = **безшовний темний космос** (fixed `CosmicBackground` — starfield canvas + туманності + blueprint-grid) з блоками, що плавають без видимих переходів. Frosted-glass картки (AuthKit-стиль). Секції: HERO → WHO (архетипи) → FEATURES (переваги) → FORMAT → AUTHOR (Вадим) → PRICING → FAQ → FOOTER.

**Головний елемент — мандала-герой** (`RoamingMandala.tsx`): лого-квітка, що **горить** (CSS-вогонь + ембер-сяйво) і **мандрує по коловій орбіті** на скролі, завжди повертаючись «обличчям» до центру екрана, частиною виходячи за край. 3D через CSS-екструзію (6 шарів). Рух — GPU-трансформації (translate/rotate), НЕ left/top.

**Ціни:** блок «Учасник клубу», цінність ~~5 000 грн~~ → **25 € / міс (≈ 1 200 грн)**, список переваг, CTA → Telegram-бот.

**Тексти винесено в один файл:** `src/content.ts` (Вадим редагує сам; інструкція — `DEPLOY.md`). Копію переписано на «продажну» (біль→результат, голос Вадима від 1-ї особи).

## Ключові файли
- `src/content.ts` — УСІ тексти головної (редагується вручну)
- `src/app/page.tsx` — головна (бере тексти з content.ts, іконки/структура в коді)
- `src/components/RoamingMandala.tsx` — вогняна мандала-герой (мобільна легка гілка + десктоп 3D)
- `src/components/CosmicBackground.tsx` — фон-космос (на мобільному зорі статичні)
- `src/components/SmoothScroll.tsx` — Lenis (вимкнено на тач/мобільному)
- `src/app/globals.css` — дизайн-токени, `.frost`, вогняні keyframes, мобільні оверайди
- `src/app/icon.svg` — фавіконка (чорна мандала на білому)
- `public/logo-mandala.svg` (градієнт), `logo-mandala-white.svg` (нав/футер), `logo-mandala-fire.svg`, `photos/vadym.png`
- `src/app/metod-432/page.tsx` — флагман з R3F 3D-героєм (окрема сторінка)
- `src/lib/analytics.ts` — GA4/Pixel ID + хелпери `track` / `trackSubscribeClick`
- `src/components/Analytics.tsx` — завантаження GA4 + Meta Pixel; `src/components/CtaLink.tsx` — кнопка CTA з подією
- `src/lib/seo.ts`, `src/app/opengraph-image.tsx`, `src/app/sitemap.ts`, `src/app/robots.ts` — SEO
- `ANALYTICS.md` — що і як міряється, що натиснути в Meta/GA/Vercel

## Дизайн-система (коротко)
- Космос: `--c432-bg #0D0E2D`, deep `#07081B`; акцент золото-помаранч `#FDD16F→#EF8018`; вторинний фіолет `#6D5AE6`
- Шрифти: **Fraunces** (дисплей, `.font-display`) + **Montserrat** (текст/UI)
- Картки: `.frost` (frosted glass; на мобільному backdrop-blur вимкнено — дорого)
- Стиль-орієнтир: `~/Downloads/DESIGN.md` (AuthKit frosted-cathedral)
- Скіл `ui-ux-pro-max` (у `.claude/skills/`) + Magic MCP підключені

## Мобільна продуктивність (важливі рішення)
Раніше прибрано: Lenis на тачі, рух мандали через left/top → transform, 11 blur-шарів мандали → 6 (на мобільному 1). Зорі перемальовуються після resize.

**Другий підхід, 2026-08-20** — заміряно й виправлено ще пʼять причин (усі в `@media (max-width:820px)` у globals.css):
1. **`backdrop-filter` насправді ніколи не вимикався на телефоні.** Перебивання писало `backdrop-filter:none` + `-webkit-`, і мініфікатор викидав НЕпрефіксний рядок. Тепер блюр не «вимикається на мобільному», а **вмикається лише від 821px** (`@media (min-width:821px)`). Не повертай перебивання через `none !important` — воно не працює.
2. **Три туманності** мали `blur(120–130px)` + нескінченну анімацію `breathe` (scale+opacity) → браузер перераховував повноекранне розмиття щокадру. На мобільному `.nebula { filter:none; animation:none }`.
3. **`.btn-cta`** анімувала `box-shadow` (властивість перемальовування) на 4 кнопках безперервно. Вимкнено.
4. **`.mandala-front`** — два `drop-shadow` на шарі, що постійно обертається. Фільтр вимкнено (сяйво лишається від радіальних градієнтів під мандалою).
5. Тінь карток `.frost` спрощено з 4 шарів до 2.

**Завантаження:**
- **Текст першого екрана більше не залежить від JS.** Був `opacity:0` в HTML і проявлявся лише після гідратації React + framer-motion → на повільному телефоні кілька секунд порожнього екрана. Тепер поява через CSS (`.rise-in` + `.rise-d1/2/3` у globals.css). **Не повертай framer-motion у hero.**
- Шрифти self-hosted через `next/font` (було render-blocking до fonts.googleapis.com + друге зʼєднання до fonts.gstatic.com).
- Meta Pixel — найважчий ресурс сторінки (`fbevents.js` 105 КБ + конфіг 119 КБ ≈ 2/3 ваги). Прибрати не можна, потрібен для реклами. Довгострокове рішення — Conversions API (серверні події).

⚠️ Tailwind-утиліти з `!` (напр. `!text-[13px]`) **перебивають** твої `!important` у globals.css — через шари CSS important-правила поза шаром мають НАЙнижчий пріоритет. Розміри в шапці правити класами в `page.tsx`, не в CSS.

---

## 🗺️ РОАДМАП (узгоджено з Вадимом — робити далі)

### P1 — першими (макс. віддача)
1. ~~**Аналітика + Meta Pixel**~~ ✅ **зроблено 2026-08-20.** GA4 `G-VSKQWFFNLK` + Meta Pixel `406163265532535` (ID взяті зі старого Weblium-сайту) + Vercel Analytics/Speed Insights. Подія кліку «Стати учасником»: GA4 `subscribe_click`, Meta `Lead`, з параметром `location` (nav/hero/pricing/footer) і `value 25 EUR`. Деталі й що натиснути в кабінетах Meta/GA — **`ANALYTICS.md`**.
2. ~~**Відгуки / соц.доказ**~~ ✅ **2026-08-24.** Секція «Голоси клубу»: **8 відгуків** з виділеним ключовим результатом у кожному + цитата Інни під ними. Тексти в `content.ts` → `voices`. У блоці автора «∞» замінено на **311** лекцій (реальна кількість із клубним доступом). ❗ Фото до відгуків НЕ ставимо, поки Вадим не спитає згоди в цих людей.
3. ~~**SEO + OG**~~ ✅ **зроблено 2026-08-20.** Динамічна OG-картинка (`src/app/opengraph-image.tsx`, next/og + Montserrat з `src/app/_og/`), повні metadata в `layout.tsx` (metadataBase, canonical, OG/Twitter, robots), `sitemap.ts`, `robots.ts`, JSON-LD Organization + WebSite (layout) та FAQPage + Product/Offer (головна), title/description/canonical на всі сторінки.
4. ~~**Лід-магніт — тест на цінності**~~ ✅ **зроблено 2026-08-31.** Сторінка `/tsinnosti`.
   **Механіка:** не тест зі шкалами, а картковий відбір. Крок 1 — познач усе, що відгукується (89 цінностей). Крок 2 — лиши рівно 10. Саме звуження і є вправою.
   **Результат:** десятка → дві теми, навколо яких вона зібралася → **справжні питання з чату** + **справжні назви лекцій із бази** (усі звірені, жодної вигаданої) → CTA у бот.
   **Не ховаємо за контактом** — результат показується повністю, пропозиція вступити йде після.
   **Файли:** `src/values.ts` (усі тексти, 89 цінностей із тегом теми, 6 тем із питаннями й лекціями), `src/app/tsinnosti/` (сторінка + компонент). Вхід із головної — блок `testTeaser` перед цінами.
   **Події:** `values_test_start`, `values_test_complete`, CTA з мітками `values_join`, `values_save`, `values_lesson`.
   **Збереження в бот (2026-09-02):** кнопка «Зберегти результат у боті» несе десятку в діп-лінку `?start=vals-<версія>-<10 байтів base64url>` (21 символ, ліміт Telegram — 64). Бот розшифровує і пише в **MongoDB → колекція `users` → поле `valuesTests`** (масив: `{version, indices, theme, source, takenAt}`). Масив, а не одне поле: так лишається історія проходжень і буде з чого рахувати збіги між учасниками.
   ⚠️ **Список цінностей продубльовано** у `bot/src/data/values432.js` (згенеровано з `src/values.ts`). Міняєш склад цінностей → підніми `VALUES_VERSION` у values.ts, перегенеруй файл бота. Бот відкидає payload з чужою версією, тож старі посилання не розшифруються криво.
   **Файли бота:** `data/values432.js`, `handlers/botExternalDataHandler.js` (гілка `vals-`), `handlers/botUserNameHandler.js` (після реєстрації), `data/constants.js`, `data/messages.js` (`VALUES_SAVED`), `mongoClient.js` (`addValuesTest`).
   ⚠️ Не повертай `AnimatePresence mode="wait"` у ValuesTest: він чекає завершення анімації зникнення, і при пригальмованих кадрах тест зависає назавжди.
   ⚠️ Список цінностей **свій**, не з тієї статті: там дублі («Самодостатність» тричі), наповнювач («Водний баланс») і 300 пунктів, які на телефоні не переглянути.
5. **Відео 60–90 сек від Вадима** в блоці автора — Вадим планує зняти.

### P2
5. Навігація в шапці (меню + бургер на мобільному) — єдине, що лишилось з P2, окрім сторінок продуктів
6. ~~Блок «як проходить місяць у клубі»~~ ✅ **2026-08-20.** Секція «Твій перший місяць, тиждень за тижнем»: 4 картки (Фундамент / Твоя ситуація / Практика / Інтеграція) + підсумковий рядок. Тексти в `content.ts` → `month`.
7. Решта сторінок продуктів: Ліла (школа+гра — є арт ігрового поля в архіві), Ретрит, 7 таємниць
8. ~~FAQ — обробка заперечень~~ ✅ **2026-08-20.** 8 питань замість 4 довідкових: не підійде / немає часу / оплата й скасування / секта / вже дивився лекції / підготовка / чому 25 € / як технічно. **Про повернення коштів нічого не обіцяно свідомо** — див. хвіст про оферту.

### P3 (полірування)
- Справжнє 3D мандали на R3F (реакція на курсор)
- Тестимоніали-карусель, мікро-анімації, cookie-згода (EU), Lighthouse-аудит, контакт для підтримки

## 📊 Статистика клубу (зняте 2026-08-31, НЕ для сайту)
База бота веде облік лише з 20.01.2025, тому це не «за 4 роки».
- **107** коли-небудь мали клубну підписку, **19** активні, 88 припинили
- Перший вхід за роками: 2024 — 33 (проставлено вручну), 2025 — 51, 2026 — 23
- **Тримаються: медіана 2 місяці**, середнє 4
- Розподіл: до 1 міс — **38**, 1–3 — 11, 3–6 — 14, 6–12 — 6, 12+ — 9

⚠️ **Вузьке місце — перший місяць:** половина новеньких іде, не дочекавшись другого. Хто перетнув третій місяць, лишається надовго. Тому текст «один місяць це знайомство, зміни на 2–3-му» б'є в реальну проблему, а не просто гарно звучить.
⚠️ **Цифру «скільки пройшло через клуб» на сайт не ставимо** — рішення Вадима: 107 проти 19 у групі породжує питання «чому не лишаються», на яке він не хоче відповідати.

## 🔧 Відкриті хвости
- [x] ~~**Оферта не відповідає тому, що продаємо**~~ ✅ **2026-08-21.** `/oferta` переписано під підписку (15 розділів: регулярні списання і згода на них, валюта, скасування, повернення 14 днів на перший платіж, запис зустрічей і конфіденційність учасників, застереження що це НЕ медична/психотерапевтична допомога, ІВ, правила спільноти, форс-мажор). Старий текст про ретрит переїхав на `/oferta-retreat`. **Не перевірено юристом.**
- [x] ~~Реквізити в оферті~~ ✅ підтверджено Вадимом: пошта `prosto.people.ua@gmail.com`, рахунок Універсал Банку. Оновлено на всіх трьох юридичних сторінках.
- [ ] **Колаж фото учасників прибрано (2026-08-31)** — публікувати впізнавані обличчя без згоди кожного не можна. Повертати лише зі знеособленими кадрами або з реальними дозволами.
- [x] ~~Підтвердити лічильники~~ ✅ **2026-08-20.** Meta Pixel `406163265532535` (бізнес Prosto Mindful) отримує `PageView` і `Lead` з club432.com — перевірено з телефона (Safari). ⚠️ У Chrome Вадима блокувальник ріже запити до `facebook.com/tr`: Pixel Helper бачить піксель, але подій немає. Це не баг сайту, перевіряти тільки з телефона або в чистому браузері.
- [ ] **GA4: позначити `subscribe_click` як Key event** (Admin → Events), дані зʼявляються за добу
- [ ] **Увімкнути Analytics/Speed Insights** у кабінеті Vercel для проєкту `retreat-landing`
- [ ] **Google Search Console** — додати club432.com і подати `https://club432.com/sitemap.xml`
- [ ] **Нове фото Вадима** — Вадим надсилав вкладенням у чат (не файл). Треба зберегти як `~/Downloads/PROSTO/vadym.png` → скопіювати в `public/photos/vadym.png` → деплой. (Поточне фото — старе з архіву, обрізане; вставлене в скляну картку.)
- [ ] **Форми → бот** — ліди з сайту слати в Telegram (`botService.notifyAdmins`, Вадим `547633690`)
- [ ] Винести тексти `/oferta` і `/metod-432` у `content.ts`

## Рекомендований старт нової сесії
P1.1, P1.3, P2.6 і P2.8 закриті. Далі — **P1.2 (відгуки/соц.доказ)** і **P1.4 (лід-магніт)**: обидва впираються в матеріали від Вадима (3–6 відгуків; рішення, що саме віддаємо безкоштовно). Третій великий пункт — **Conversions API** (серверні події в Meta замість браузерних): обходить блокувальники й обмеження iOS, Meta обіцяє помітно дешевший результат. Усе для нього є (VPS + бот).

## Архів контенту (для нових сторінок)
Повна копія старого Weblium-сайту: `~/work/club432-site/` (16 стор. + 329 зображень, у т.ч. арт ігрового поля Ліли, фото). Скрипти дзеркала — у scratchpad `mirror.py`/`pass2_images.py`.

## Промені (езотерична психологія) в тесті на цінності

Додано 03.09.2026. Джерело: А. А. Бейлі «Езотерична психологія», тексти Вадима в Notion.

**Головне, що треба розуміти перед правками.** У кожного Променя два протилежні
переліки якостей: `strengths` (те, чим він уже є) і `growth` (те, чого йому бракує).
Терпіння у Другого стоїть у рисах, а у Першого в тому, що треба набути. Тому одне
слово вказує на різні Промені залежно від читання. Ми рахуємо обидві шкали:

- **відгук** — скільки обраних цінностей лежить у `strengths`;
- **потреба** — скільки лежить у `growth`.

Збіг = сильний сигнал. Розходження = найцінніше в результаті, і саме воно веде
до питання, з яким людина приходить у клуб.

**Нормування обовʼязкове.** Слова розподілені по Променях нерівно (у Пʼятого серед
рис їх 20, у Третього 8), бо таким є першоджерело. Сирі влучання дали б перемогу
Пʼятому просто частотою. Рахуємо lift = hits / expected. Після нормування розподіл
на випадкових проходженнях 11–16% на Промінь замість перекосу.

**Файли:**
- `src/rays.ts` — сім Променів + `readRays()`. Уся механіка описана в шапці файлу.
- `src/values.ts` — цінності з тегами `res` / `need`. Версія піднята до 2, бо додано
  7 слів (не вистачало покриття Четвертого і Шостого Променів, тест їх недобирав).
- `scripts/gen-bot-values.mjs` — генерує `bot/src/data/values432.js`. **Запускати
  щоразу після зміни складу**, інакше бот розшифрує діп-лінки старим списком.
  Шлях до бота заданий явно, бо retreat-landing лежить у `~/432`, а бот у `~/work`.

**Межа, яку не переходити.** Це натяк, а не діагноз. Повна променева структура це
пʼять позицій (Душа, особистість, ментальне, емоційне, фізичне тіла), з десяти слів
її не визначити. Усі формулювання в `explore` тримати на рівні «що варто дослідити
в собі», ніколи «твій Промінь Душі це».

**Рішення Вадима 03.09.2026:**
- Промені на публічному сайті НЕ показуємо: `SHOW_RAYS_ON_SITE = false` у `values.ts`.
  Публічний тест ловить холодний трафік із реклами, езотеричний шар там передчасний.
  Код блоку живий і вмикається однією зміною прапорця.
- Головна шкала для людини це **потреба**, «відгук» другим планом. Портрет Променя
  і питання для дослідження беруться саме з Променя потреби.
- У боті Промені бачать ЛИШЕ учасники клубу (перевірка активної підписки в
  `botExternalDataHandler`). Решта отримує `VALUES_RAYS_LOCKED`: це стає причиною зайти.
- У базу променевий профіль пишеться ЗАВЖДИ (`valuesTests.rayNeed` / `rayResonance` /
  `raysAligned`), незалежно від показу. Це і є оцифрування групи.

**Свідомо НЕ взято:** приклади історичних осіб із першоджерела. Там серед прикладів
Сталін, Ленін, Троцький, Чингіс Хан. Для українського сайту це неприйнятно.
Якщо приклади колись знадобляться, брати лише Конфуція, Арістотеля, Жанну дʼАрк,
Матір Терезу.

## Відео від Вадима на головній

Додано 25.09.2026. Блок `#video` одразу під першим екраном, компонент `src/components/IntroVideo.tsx`, тексти в `content.ts` → `video`.

- **mp4 лежать на VPS, не у Vercel:** `/var/www/club432-media/` → `https://app.club432.com/media/…` (location `/media/` у `/etc/nginx/sites-available/club432.conf`, бекап конфігу поруч `.bak-media-20260925`). Причина: 40–70 МБ на перегляд з'їли б ліміт трафіку Vercel. Папка окрема від вебапу, бо деплой вебапу робить `rsync --delete`.
- Файли: `club432-intro-1080p.mp4` (68 МБ, десктоп) і `club432-intro-720p.mp4` (26 МБ, екрани ≤820px). Обкладинка і субтитри в `public/video/`.
- До кліку вантажиться тільки обкладинка, `<video>` з'являється після натискання. Не вмикати autoplay і preload.
- Аналітика: `video_play` (location, quality) і `video_complete`.
- Вихідники й монтаж: `~/Movies/club432-video/` (4K-майстер у `final-short/`, музика 432 Гц синтезована скриптом `music/synth432.py`, прав третіх осіб нема).
