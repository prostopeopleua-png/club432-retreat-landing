# Як змінювати тексти та публікувати сайт club432.com

## 1. Змінити тексти

Усі тексти головної сторінки — в одному файлі:

```
~/work/retreat-landing/src/content.ts
```

Відкрий його будь-яким текстовим редактором (VS Code, TextEdit, Sublime…).

**Правила, щоб нічого не зламати:**
- Міняй текст **тільки між лапками** `"..."`.
- **Не видаляй** лапки `"`, коми `,`, дужки `[ ] { }`.
- Потрібен перенос рядка всередині тексту — постав `\n`.
- Символ `€` і подібні — можна вставляти вільно.

Приклад — було:
```
price: "25 €",
```
стало:
```
price: "30 €",
```

Збережи файл (Cmd+S).

## 2. Опублікувати (задеплоїти)

Відкрий **Термінал** і виконай дві команди:

```bash
cd ~/work/retreat-landing
vercel --prod --yes
```

Зачекай ~1 хвилину — унизу зʼявиться `Production: … READY`. Сайт **club432.com** оновиться автоматично.

> Якщо в тексті випадково зламав лапку/кому — Vercel покаже помилку збірки й **старий сайт залишиться цілим** (нова версія не опублікується, поки не виправиш). Це безпечно.

## 3. (Необовʼязково) Переглянути локально перед публікацією

```bash
cd ~/work/retreat-landing
npm run dev
```
Відкрий у браузері `http://localhost:3000`. Зупинити — `Ctrl+C` у терміналі.

---

**Що де в `content.ts`:**
- `hero` — перший екран (заголовок, підзаголовок, кнопки)
- `who` — блок «Впізнаєш себе» (3 архетипи)
- `features` — «Що ти отримуєш» (5 переваг)
- `format` — формат клубу (3 пункти)
- `author` — про Вадима (цитата, біо, цифри)
- `pricing` — ціна та список переваг підписки
- `faq` — часті питання
- `footer` — підвал (посилання)
- `botUrl` — куди ведуть усі кнопки «Стати учасником»
